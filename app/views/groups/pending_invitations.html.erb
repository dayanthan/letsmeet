<%- model_class = Group -%>
<div class="page-header">
  <h1>pending invitations</h1>
</div>

<div class="pendingList"%>
  <%= render partial: 'pending_list' , :locals => {:own_groups => @own_groups} %>
</div>